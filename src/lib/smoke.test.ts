import { describe, expect, it } from "vitest";

// Placeholder keeping the suite green until Phase 1 adds real tests.
describe("toolchain", () => {
  it("runs vitest", () => {
    expect(1 + 1).toBe(2);
  });
});
